{
  "name": "pocketbreathcoach-marketing-site",
  "version": "1.0.0",
  "description": "Marketing site for Pocket Breath Coach app",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
